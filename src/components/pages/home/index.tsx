import { DefaultTemplate } from '@/components/templates/default-template'

import { UserInput } from './user-input'

export const Home = () => {
  return (
    <DefaultTemplate>
      <UserInput />
    </DefaultTemplate>
  )
}
